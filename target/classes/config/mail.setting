# 邮件服务器的SMTP地址，可选，默认为smtp.<发件人邮箱后缀>
host = smtp.qq.com

# 邮件服务器的SMTP端口，可选，默认25
port = 465

# 发件人（必须正确，否则发送失败）
from = 1163743662@qq.com

# 用户名，默认为发件人邮箱前缀
user = 1163743662

# 密码（注意，某些邮箱需要为SMTP服务单独设置授权码）
pass = xclznshbpknmiaij

# 使用 STARTTLS安全连接，STARTTLS是对纯文本通信协议的扩展
starttlsEnable = true
